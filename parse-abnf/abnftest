#!/usr/bin/perl

use Data::Dumper;
require "ABNF.pm";

#print Data::Dumper::Dumper($Parse::ABNF::ABNF), "\n";
#$rule = "rule           =  rulename defined-as elements c-nl\nfoo = bar baz\n";
#$ret = $Parse::ABNF::ABNF->matches("rulelist", $rule, qw(rule rulename defined-as elements));

#print Data::Dumper::Dumper($ret), "\n";

$rules = <<EOF;
foo = bar baz
baz = ["the"] quick 1*2(brown fox) / jumped "over" %x3C.3E %d48-50 %b100000
bar = "alt1"
bar =/ "alt2"
EOF
$newobj = Parse::ABNF->new($rules);
#print Data::Dumper::Dumper($newobj), "\n";
